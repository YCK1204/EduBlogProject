# 배포 전 QA 체크리스트

> 대상: 프로그래밍 교육 블로그 (Next.js 16 정적 export, ko/en/ja, 라이트/다크)
> 사용법: 각 항목을 실제 브라우저에서 확인하고 `[ ]` → `[x]` 로 체크. 문제 발견 시 하단 **이슈 로그**에 기록.
> 권장 점검 환경: 데스크톱 Chrome / Safari / Firefox + 모바일(Chrome Android, Safari iOS)

---

## 0. 빌드 / 배포 산출물

- [ ] `npm run build` 가 에러 없이 완료된다 (`out/` 생성)
- [ ] `npm run lint` 통과 (또는 남은 경고가 의도된 것인지 확인)
- [ ] `out/` 를 정적 서버로 띄워(예: `npx serve out`) 프로덕션과 동일 조건에서 확인
- [ ] 32개 레슨 페이지가 모두 정적 생성됨 (`out/category/.../index.html` 존재)
- [ ] 홈 / 4개 카테고리 목록 페이지가 정상 생성됨
- [ ] `trailingSlash` 동작 확인 — `/category/algorithms` 와 `/category/algorithms/` 둘 다 정상
- [ ] 404 페이지가 정상 동작 (존재하지 않는 slug 접근 시)
- [ ] `console`에 에러/경고가 쏟아지지 않는다 (특히 hydration mismatch)

## 1. 개발 전용 기능 노출 차단 (프로덕션 빌드에서 확인) ⚠️중요

- [ ] `/dev/editor/` 직접 접근 시 에디터 UI가 **보이지 않는다** (404/빈 화면)
- [ ] 레슨 페이지에 **DevEditButton(편집 버튼)이 노출되지 않는다**
- [ ] 카테고리/홈 등에 개발용 "새 레슨" 버튼류가 노출되지 않는다
- [ ] 블록 위 `...` 인라인 편집 메뉴가 프로덕션에서 뜨지 않는다
- [ ] `/api/dev/*` 호출 흔적이 없다 (네트워크 탭 확인)

## 2. 다국어 (i18n) — ko / en / ja

- [ ] 우하단 언어 FAB(또는 드롭다운)로 ko ↔ en ↔ ja 전환된다
- [ ] 언어 선택이 새로고침/페이지 이동 후에도 유지된다 (localStorage)
- [ ] UI 문자열(헤더, 네비, 푸터, 버튼)이 선택 언어로 모두 번역된다 — **undefined/키 노출 없음**
- [ ] 레슨 제목·요약·본문이 선택 언어로 표시된다
- [ ] **폴백 동작**: ja 콘텐츠 없는 레슨에서 en → ko 순으로 자연스럽게 폴백
- [ ] 카테고리 카드의 제목·요약이 언어별로 바뀐다
- [ ] 연관 주제(related) 링크 제목이 언어별로 바뀐다
- [ ] `<html lang>` 속성이 선택 언어에 맞게 갱신된다
- [ ] 첫 방문(localStorage 없음) 기본 언어가 의도대로 동작

## 3. 테마 (라이트 / 다크)

- [ ] 테마 토글 버튼으로 라이트 ↔ 다크 전환된다
- [ ] 테마가 새로고침/이동 후 유지된다 (localStorage)
- [ ] **다크 사용자 첫 진입 시 깜빡임(FOUC) 없음** — 라이트로 잠깐 보였다 바뀌지 않는다
- [ ] **다이어그램 이미지가 첫 페인트부터 올바른 테마로 표시** (라이트→다크 이미지 깜빡임 없음)
- [ ] 다크 모드에서 텍스트/배경/테두리 대비가 충분하다
- [ ] 코드 블록 하이라이트가 양쪽 테마에서 읽힌다
- [ ] 카테고리 카드 프리뷰 이미지도 테마에 맞게 표시

## 4. 레슨 콘텐츠 렌더링 (블록 타입)

- [ ] `text` / `bold` / `italic` / `underbar` / `strike` 정상 렌더
- [ ] `header` (level 1~3) 계층/크기 정상
- [ ] `points`(불릿) / `ol`(번호) 리스트 정상
- [ ] `box`(중첩 블록) 정상 렌더
- [ ] `color` 블록(사용 시) 색상 정상
- [ ] `image` 블록 — 대체 텍스트(alt) 존재, 크기 깨짐 없음
- [ ] 본문에 깨진 문자/이스케이프 잔재(`\!`, `&amp;` 등) 없음
- [ ] 긴 레슨에서 스크롤 시 좌측 사이드바 **활성 스텝 하이라이트(scroll-spy)** 정상
- [ ] 사이드바 스텝 클릭 시 해당 위치로 스크롤 이동

## 5. 코드 블록

- [ ] 언어 드롭다운(Python/JavaScript/C++/C# 등)으로 코드 전환된다
- [ ] 문법 하이라이트가 언어별로 정상 적용
- [ ] **복사 버튼** 클릭 시 클립보드 복사 + 피드백 표시
- [ ] (http 등 비보안 컨텍스트에서) 복사 실패 시 앱이 깨지지 않는다
- [ ] 가로로 긴 코드의 스크롤/줄바꿈 처리 정상
- [ ] "예시 코드" 스텝(레슨 말미)에 코드가 모여 있고 소제목이 정상

## 6. 이미지 / 다이어그램 (SVG)

- [ ] 모든 레슨의 다이어그램이 **깨짐 없이** 로드된다 (404 없음)
- [ ] 언어별 SVG 텍스트가 선택 언어와 일치 (ko=한국어, en=영어, ja=일본어)
- [ ] 다크 모드용 `-dark.svg` 가 모두 존재하고 정상 표시
- [ ] 이미지에 명시적 width/height 가 있어 레이아웃 시프트(CLS) 없음
- [ ] 느린 네트워크에서 이미지 로딩 중 레이아웃이 튀지 않는다

## 7. 네비게이션 / 라우팅

- [ ] 홈 → 카테고리 → 레슨 이동 정상
- [ ] Breadcrumb(경로) 링크 정상 동작
- [ ] 헤더 네비게이션 4개 카테고리 링크 정상
- [ ] 푸터 링크 정상
- [ ] 브라우저 뒤로/앞으로 가기 정상
- [ ] 새로고침 시 현재 페이지 유지 (정적 라우트 정상)
- [ ] 키보드 내비게이션: `↑/k` 이전 스텝, `↓/j` 다음 스텝, `Space` 완료 토글

## 8. 진행도 (Progress / localStorage)

- [ ] 레슨 완료 표시(체크)가 동작하고 새로고침 후 유지
- [ ] 카테고리/목록에서 완료 상태가 반영된다(있는 경우)
- [ ] **localStorage 차단/시크릿 모드**에서도 앱이 크래시 없이 동작 (에러 토스트는 OK)
- [ ] 손상된 localStorage 값이 있어도 앱이 깨지지 않는다

## 9. 반응형 / 레이아웃

- [ ] 320 / 375 / 768 / 1024 / 1440 / 1920 너비에서 가로 오버플로 없음
- [ ] 모바일에서 좌측 스텝 사이드바 처리(숨김/대체) 정상
- [ ] 모바일에서 언어 FAB / 테마 토글이 콘텐츠를 가리지 않는다
- [ ] 코드 블록·이미지가 좁은 화면에서 넘치지 않는다
- [ ] 터치 인터랙션(탭, 스크롤) 정상

## 10. 접근성 (a11y)

- [ ] 키보드만으로 주요 흐름 탐색 가능 (Tab 순서, 포커스 링)
- [ ] 모든 이미지에 의미 있는 `alt`
- [ ] 색 대비 WCAG AA 충족 (라이트/다크 모두)
- [ ] 시맨틱 구조(`header`/`main`/`nav`/`footer`, 헤딩 계층) 적절
- [ ] 버튼/링크에 접근성 레이블(aria-label) 존재
- [ ] `prefers-reduced-motion` 존중 (과한 애니메이션 없음)

## 11. 성능 (Core Web Vitals)

- [ ] Lighthouse(모바일) 점검: LCP < 2.5s, CLS < 0.1, TBT/INP 양호
- [ ] 첫 화면 폰트 로딩으로 인한 텍스트 깜빡임(FOIT) 과하지 않음
- [ ] 번들 크기 과다하지 않음 (불필요한 dev 코드 미포함 확인)
- [ ] 이미지가 렌더 크기 대비 과도하게 크지 않음

## 12. 보안 / 호스팅 설정

- [ ] 배포 플랫폼(Vercel/GitHub Pages)에 보안 헤더 설정:
  - [ ] `Content-Security-Policy`
  - [ ] `X-Content-Type-Options: nosniff`
  - [ ] `X-Frame-Options: DENY`
  - [ ] `Referrer-Policy: strict-origin-when-cross-origin`
  - [ ] `Strict-Transport-Security` (HTTPS)
- [ ] HTTPS 강제 / HTTP→HTTPS 리다이렉트
- [ ] 소스/번들에 하드코딩된 시크릿 없음
- [ ] `npm audit` 결과 확인 (moderate 이상 처리 또는 사유 기록)
- [ ] 외부 폰트(Google Fonts) 로드 실패 시 폴백 폰트로 정상 표시

## 13. SEO / 메타

- [ ] `<title>` / `description` 메타 정상
- [ ] OpenGraph(og:title/description/type/siteName) 정상
- [ ] 파비콘 / 아이콘 표시
- [ ] (필요 시) `sitemap.xml` / `robots.txt` 존재 및 정상
- [ ] 레슨 페이지별 적절한 메타데이터(있는 경우)

## 14. 브라우저 호환성

- [ ] Chrome (데스크톱/모바일)
- [ ] Safari (macOS/iOS) — 특히 스크롤/sticky/clipboard
- [ ] Firefox
- [ ] Edge

---

## 회귀(Regression) 스모크 — 최근 변경분 집중 확인

- [ ] 레슨 상단 태그 영역에 **"N개 스텝/챕터" 표기가 사라졌는지** 확인
- [ ] 다크모드 다이어그램 **깜빡임 제거** 재확인 (CSS 전환)
- [ ] `/dev/editor` **프로덕션 차단** 재확인
- [ ] 일본어(ja) 추가 후 ko/en 동작에 회귀 없는지 확인

---

## 이슈 로그

| # | 화면/기능 | 환경(브라우저/뷰포트/테마/언어) | 증상 | 심각도 | 상태 |
|---|-----------|--------------------------------|------|--------|------|
|   |           |                                |      |        |      |

---

### 심각도 기준
- **Blocker**: 배포 불가 (기능 깨짐/보안/데이터 손실)
- **High**: 배포 전 수정 권장
- **Medium**: 다음 릴리스에서 수정
- **Low**: 개선 사항
