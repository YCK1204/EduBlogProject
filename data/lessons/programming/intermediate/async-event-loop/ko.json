{
  "slug": "async-event-loop",
  "tag": "Async",
  "relatedSlugs": [
    "functions-first-class",
    "type-system-generics",
    "clean-code"
  ],
  "title": "비동기 처리와 이벤트 루프",
  "summary": "콜백·프로미스·async/await의 진화 과정과 JavaScript 이벤트 루프의 동작 원리를 분석합니다. 비동기 코드를 올바르게 작성하고 디버깅하는 방법을 이해합니다.",
  "steps": [
    {
      "number": 1,
      "title": "동기 vs 비동기",
      "blocks": [
        {
          "type": "image",
          "src": "/images/async-event-loop.svg",
          "alt": "Event loop asynchronous execution",
          "width": 88
        },
        {
          "type": "text",
          "content": "동기(Synchronous) 실행은 한 작업이 끝나야 다음 작업이 시작됩니다. 비동기(Asynchronous) 실행은 작업을 시작한 후 완료를 기다리지 않고 다음 코드를 실행합니다. 파일 읽기, 네트워크 요청 같은 I/O 작업은 대기 시간이 길어 비동기 처리가 효율적입니다."
        },
        {
          "type": "text",
          "content": "JavaScript는 싱글 스레드 언어입니다. 동기적으로 실행하면 네트워크 응답을 기다리는 동안 브라우저가 완전히 멈춥니다. 이벤트 루프와 비동기 API로 이 문제를 해결합니다."
        },
        {
          "type": "points",
          "items": [
            "I/O 바운드 작업: 비동기가 효율적",
            "CPU 바운드 작업: 멀티스레딩/멀티프로세싱 필요",
            "Blocking: 현재 스레드를 멈춤",
            "Non-Blocking: 작업을 요청하고 즉시 반환"
          ]
        }
      ]
    },
    {
      "number": 2,
      "title": "콜백과 콜백 헬",
      "blocks": [
        {
          "type": "text",
          "content": "콜백(Callback)은 비동기 작업 완료 후 호출될 함수를 미리 전달하는 패턴입니다. 초기 JavaScript의 기본 비동기 처리 방식입니다. setTimeout, addEventListener, fs.readFile(Node.js) 등이 콜백을 사용합니다."
        },
        {
          "type": "text",
          "content": "콜백은 중첩이 깊어질수록 코드가 오른쪽으로 들여써지는 '콜백 헬(Callback Hell)' 또는 '피라미드 오브 둠'이 발생합니다. 에러 처리도 각 단계마다 해야 하고, 코드 흐름을 추적하기 어려워집니다."
        },
        {
          "type": "points",
          "items": [
            "콜백 패턴: fs.readFile(path, (err, data) => {})",
            "에러 우선 콜백(Node.js): 첫 인수가 에러",
            "콜백 헬: 중첩된 콜백 → 읽기 어렵고 오류 처리 복잡",
            "이벤트 이미터 패턴: on('event', callback)"
          ]
        }
      ]
    },
    {
      "number": 3,
      "title": "Promise와 async/await",
      "blocks": [
        {
          "type": "text",
          "content": "Promise는 비동기 작업의 미래 결과를 나타내는 객체입니다. Pending(대기) → Fulfilled(성공)/Rejected(실패) 세 가지 상태를 가집니다. .then()으로 성공 처리, .catch()로 에러 처리를 체이닝할 수 있어 콜백 헬이 해소됩니다."
        },
        {
          "type": "text",
          "content": "async/await는 Promise를 동기 코드처럼 보이게 작성할 수 있는 문법 설탕입니다. async 함수 내에서 await로 Promise 결과를 기다립니다. try/catch로 에러를 처리할 수 있어 동기 코드 스타일로 비동기 로직을 표현할 수 있습니다."
        },
        {
          "type": "points",
          "items": [
            "Promise: 미래 값의 컨테이너 (then/catch/finally)",
            "Promise.all: 여러 비동기 병렬 실행, 모두 완료 대기",
            "async/await: Promise의 문법 설탕 — 더 읽기 쉬움",
            "await 없이 async: Promise 반환만 하고 기다리지 않음 (실수 주의)"
          ]
        }
      ]
    },
    {
      "number": 4,
      "title": "이벤트 루프의 동작",
      "blocks": [
        {
          "type": "text",
          "content": "JavaScript 이벤트 루프는 콜 스택, 태스크 큐(Macro Task), 마이크로태스크 큐로 구성됩니다. 콜 스택이 비면 마이크로태스크 큐(Promise.then) 전부 처리 후, 태스크 큐(setTimeout, I/O) 하나를 실행합니다."
        },
        {
          "type": "text",
          "content": "이 순서를 이해하면 비동기 코드의 실행 순서를 예측할 수 있습니다. setTimeout(fn, 0)은 즉시 실행되지 않고 콜 스택이 비어야 실행됩니다. Promise.then은 setTimeout보다 먼저 실행됩니다(마이크로태스크 우선)."
        },
        {
          "type": "points",
          "items": [
            "콜 스택: 현재 실행 중인 코드",
            "마이크로태스크: Promise.then, queueMicrotask → 스택 빌 때마다 전부 처리",
            "태스크 큐: setTimeout, setInterval, I/O → 마이크로태스크 후 하나씩",
            "실행 순서: 동기 → 마이크로태스크 → 태스크"
          ]
        }
      ]
    },
    {
      "number": 5,
      "title": "예시 코드",
      "blocks": [
        {
          "type": "header",
          "level": 3,
          "content": "Promise와 async/await"
        },
        {
          "type": "code",
          "javascript": "// Promise 체이닝\nfetch('/api/user')\n  .then(res => res.json())\n  .then(user => fetchPosts(user.id))\n  .then(posts => console.log(posts))\n  .catch(err => console.error(err));\n\n// async/await — 같은 로직, 더 읽기 쉽게\nasync function loadUserPosts() {\n  try {\n    const res  = await fetch('/api/user');\n    const user = await res.json();\n    const posts = await fetchPosts(user.id);\n    console.log(posts);\n  } catch (err) {\n    console.error(err);\n  }\n}\n\n// 병렬 실행\nconst [user, settings] = await Promise.all([\n  fetchUser(id),\n  fetchSettings(id),\n]);",
          "python": "import asyncio\nimport aiohttp\n\n# async/await (Python 3.5+)\nasync def fetch_user(session, user_id):\n    async with session.get(f'/api/users/{user_id}') as resp:\n        return await resp.json()\n\nasync def main():\n    async with aiohttp.ClientSession() as session:\n        # 병렬 실행\n        users = await asyncio.gather(\n            fetch_user(session, 1),\n            fetch_user(session, 2),\n        )\n        print(users)\n\nasyncio.run(main())",
          "java": "import java.util.concurrent.CompletableFuture;\n\n// CompletableFuture (Java 8+)\nCompletableFuture<User> futureUser = CompletableFuture\n    .supplyAsync(() -> fetchUser(userId))\n    .thenApply(user -> enrichUser(user))\n    .exceptionally(ex -> defaultUser());\n\n// 병렬 실행\nCompletableFuture<Void> all = CompletableFuture.allOf(\n    fetchUserAsync(id),\n    fetchSettingsAsync(id)\n);\nall.join();",
          "csharp": "// async/await (C# 5+)\nasync Task<User> LoadUserPostsAsync(int userId) {\n    try {\n        var user  = await FetchUserAsync(userId);\n        var posts = await FetchPostsAsync(user.Id);\n        return user with { Posts = posts };\n    } catch (HttpRequestException ex) {\n        Console.WriteLine(ex.Message);\n        throw;\n    }\n}\n\n// 병렬 실행\nvar (user, settings) = await (\n    FetchUserAsync(id),\n    FetchSettingsAsync(id)\n).WhenAll();"
        }
      ]
    }
  ]
}