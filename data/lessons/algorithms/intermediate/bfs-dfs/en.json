{
  "slug": "bfs-dfs",
  "tag": "Graph Search",
  "relatedSlugs": [
    "recursion",
    "dynamic-programming",
    "dijkstra"
  ],
  "title": "BFS & DFS Traversal",
  "summary": "BFS (breadth-first search) and DFS (depth-first search) are the two core algorithms for traversing graphs and trees. Compare their implementations and learn which problems each is best suited for.",
  "steps": [
    {
      "number": 1,
      "title": "What is Graph Traversal?",
      "blocks": [
        {
          "type": "text",
          "content": "Graph traversal visits all vertices reachable from a starting vertex. The key question is 'in what order?' — this difference defines BFS vs DFS."
        },
        {
          "type": "text",
          "content": "Without tracking visited vertices, cycles cause infinite loops. A visited set (or array) ensures each vertex is processed only once."
        },
        {
          "type": "points",
          "items": [
            "Purpose: connectivity check, path finding, cycle detection",
            "visited set: O(1) lookup for visit status",
            "Time complexity: adjacency list O(V+E), adjacency matrix O(V²)",
            "Trees have no cycles → BFS/DFS can skip the visited check"
          ]
        }
      ]
    },
    {
      "number": 2,
      "title": "BFS — Breadth-First Search",
      "blocks": [
        {
          "type": "image",
          "src": "/images/bfs-dfs-en.svg",
          "alt": "BFS vs DFS traversal order",
          "width": 88
        },
        {
          "type": "text",
          "content": "BFS uses a queue to visit vertices in order of their distance from the start. All vertices at distance d are visited before any at distance d+1."
        },
        {
          "type": "text",
          "content": "BFS's most important property is shortest path guarantee. The first time BFS reaches a vertex in an unweighted graph, it has found the shortest path."
        },
        {
          "type": "points",
          "items": [
            "Data structure: Queue (FIFO)",
            "Visit order: distance 1 → distance 2 → distance 3 ...",
            "Shortest path guarantee: for unweighted graphs",
            "Level-by-level processing: handle each layer before the next"
          ]
        }
      ]
    },
    {
      "number": 3,
      "title": "DFS — Depth-First Search",
      "blocks": [
        {
          "type": "text",
          "content": "DFS uses a stack (or recursion) to go as deep as possible in one direction before backtracking to explore other branches."
        },
        {
          "type": "text",
          "content": "Recursive DFS is very intuitive — the call stack naturally tracks the current path. DFS is used for cycle detection, topological sort, and SCC."
        },
        {
          "type": "points",
          "items": [
            "Data structure: Stack (or recursion call stack)",
            "Visit order: go deep in one direction, then backtrack",
            "Path tracking: maintain the set of currently visited vertices",
            "Applications: cycle detection, topological sort, SCC, backtracking"
          ]
        }
      ]
    },
    {
      "number": 4,
      "title": "BFS vs DFS — When to Use Which",
      "blocks": [
        {
          "type": "text",
          "content": "Use BFS for: shortest path, minimum moves, level-by-level processing. Use DFS for: path existence, exhaustive search, backtracking."
        },
        {
          "type": "text",
          "content": "Memory also differs. BFS queues all vertices at the same distance (up to O(V)). DFS uses O(h) stack space where h is the tree height."
        },
        {
          "type": "points",
          "items": [
            "Shortest path (unweighted): BFS",
            "Path existence, exhaustive search, backtracking: DFS",
            "BFS memory: O(V) — large on wide graphs",
            "DFS memory: O(h) — watch for stack overflow on deep trees"
          ]
        }
      ]
    },
    {
      "number": 5,
      "title": "Example Code",
      "blocks": [
        {
          "type": "header",
          "level": 3,
          "content": "BFS — Breadth-First Search"
        },
        {
          "type": "code",
          "python": "from collections import deque\n\ndef bfs(graph, start):\n    visited = set([start])\n    queue = deque([start])\n    while queue:\n        node = queue.popleft()\n        print(node)\n        for neighbor in graph[node]:\n            if neighbor not in visited:\n                visited.add(neighbor)\n                queue.append(neighbor)",
          "javascript": "function bfs(graph, start) {\n  const visited = new Set([start]);\n  const queue = [start];\n  while (queue.length) {\n    const node = queue.shift();\n    console.log(node);\n    for (const neighbor of graph[node]) {\n      if (!visited.has(neighbor)) {\n        visited.add(neighbor);\n        queue.push(neighbor);\n      }\n    }\n  }\n}",
          "java": "void bfs(Map<Integer, List<Integer>> graph, int start) {\n    Set<Integer> visited = new HashSet<>();\n    Queue<Integer> queue = new LinkedList<>();\n    visited.add(start);\n    queue.offer(start);\n    while (!queue.isEmpty()) {\n        int node = queue.poll();\n        System.out.println(node);\n        for (int neighbor : graph.getOrDefault(node, List.of())) {\n            if (!visited.contains(neighbor)) {\n                visited.add(neighbor);\n                queue.offer(neighbor);\n            }\n        }\n    }\n}",
          "c": "#include <stdio.h>\n#include <stdlib.h>\n#include <stdbool.h>\n\nvoid bfs(int graph[][MAX_V], int V, int start) {\n    bool visited[MAX_V] = {false};\n    int queue[MAX_V];\n    int front = 0, rear = 0;\n    \n    visited[start] = true;\n    queue[rear++] = start;\n    \n    while (front < rear) {\n        int node = queue[front++];\n        printf(\"%d \", node);\n        for (int neighbor = 0; neighbor < V; neighbor++) {\n            if (graph[node][neighbor] && !visited[neighbor]) {\n                visited[neighbor] = true;\n                queue[rear++] = neighbor;\n            }\n        }\n    }\n}",
          "cpp": "#include <iostream>\n#include <vector>\n#include <queue>\n#include <unordered_set>\nusing namespace std;\n\nvoid bfs(vector<vector<int>>& graph, int start) {\n    unordered_set<int> visited;\n    queue<int> q;\n    \n    visited.insert(start);\n    q.push(start);\n    \n    while (!q.empty()) {\n        int node = q.front();\n        q.pop();\n        cout << node << \" \";\n        \n        for (int neighbor : graph[node]) {\n            if (visited.find(neighbor) == visited.end()) {\n                visited.insert(neighbor);\n                q.push(neighbor);\n            }\n        }\n    }\n}",
          "csharp": "using System;\nusing System.Collections.Generic;\n\npublic static void BFS(Dictionary<int, List<int>> graph, int start) {\n    HashSet<int> visited = new HashSet<int>();\n    Queue<int> queue = new Queue<int>();\n    \n    visited.Add(start);\n    queue.Enqueue(start);\n    \n    while (queue.Count > 0) {\n        int node = queue.Dequeue();\n        Console.Write(node + \" \");\n        \n        if (graph.ContainsKey(node)) {\n            foreach (int neighbor in graph[node]) {\n                if (!visited.Contains(neighbor)) {\n                    visited.Add(neighbor);\n                    queue.Enqueue(neighbor);\n                }\n            }\n        }\n    }\n}"
        },
        {
          "type": "header",
          "level": 3,
          "content": "DFS — Depth-First Search"
        },
        {
          "type": "code",
          "python": "def dfs(graph, node, visited=None):\n    if visited is None:\n        visited = set()\n    visited.add(node)\n    print(node)\n    for neighbor in graph[node]:\n        if neighbor not in visited:\n            dfs(graph, neighbor, visited)",
          "javascript": "function dfs(graph, node, visited = new Set()) {\n  visited.add(node);\n  console.log(node);\n  for (const neighbor of graph[node]) {\n    if (!visited.has(neighbor)) {\n      dfs(graph, neighbor, visited);\n    }\n  }\n}",
          "java": "void dfs(Map<Integer, List<Integer>> graph, int node, Set<Integer> visited) {\n    visited.add(node);\n    System.out.println(node);\n    for (int neighbor : graph.getOrDefault(node, List.of())) {\n        if (!visited.contains(neighbor)) {\n            dfs(graph, neighbor, visited);\n        }\n    }\n}",
          "c": "#include <stdio.h>\n#include <stdbool.h>\n\nvoid dfs(int graph[][MAX_V], int V, int node, bool visited[]) {\n    visited[node] = true;\n    printf(\"%d \", node);\n    \n    for (int neighbor = 0; neighbor < V; neighbor++) {\n        if (graph[node][neighbor] && !visited[neighbor]) {\n            dfs(graph, V, neighbor, visited);\n        }\n    }\n}",
          "cpp": "#include <iostream>\n#include <vector>\n#include <unordered_set>\nusing namespace std;\n\nvoid dfs(vector<vector<int>>& graph, int node, unordered_set<int>& visited) {\n    visited.insert(node);\n    cout << node << \" \";\n    \n    for (int neighbor : graph[node]) {\n        if (visited.find(neighbor) == visited.end()) {\n            dfs(graph, neighbor, visited);\n        }\n    }\n}",
          "csharp": "using System;\nusing System.Collections.Generic;\n\npublic static void DFS(Dictionary<int, List<int>> graph, int node, HashSet<int> visited) {\n    visited.Add(node);\n    Console.Write(node + \" \");\n    \n    if (graph.ContainsKey(node)) {\n        foreach (int neighbor in graph[node]) {\n            if (!visited.Contains(neighbor)) {\n                DFS(graph, neighbor, visited);\n            }\n        }\n    }\n}"
        }
      ]
    }
  ]
}